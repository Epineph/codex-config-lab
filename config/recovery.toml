# Minimal baseline: let the installed client choose its supported default model.
# Keep global keys above every [table]. Do not append global keys at the end.
approval_policy = "on-request"
sandbox_mode = "workspace-write"

[sandbox_workspace_write]
network_access = false
